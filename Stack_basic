#include<bits/stdc++.h>//print all elements of stack
using namespace std;
class Stack
{
    vector<int>s;
    public:
    void push(int x)
    {
        s.push_back(x);
    }
    void pop()
    {
        s.pop_back();
    }
    int top()
    {
        if(!s.empty())
        {
            return s[s.size()-1];
        }
        return -1;
    }
    bool empty()
    {
        return s.empty();
    }
};
int main()
{
    Stack st;
    st.push(12);
    st.push(14);
    st.push(18);
while(!st.empty())
{
    cout<<st.top()<<" "; 
    st.pop();
}
return 0;
}
